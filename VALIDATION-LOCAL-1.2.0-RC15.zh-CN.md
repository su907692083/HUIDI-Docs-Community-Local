# HUIDI Docs Community Local V1.2.0 RC15 验证记录

## 范围

RC14 → RC15 发布阻断回归修复：More 抽屉挂载可见性与 Community Local 正式 PDF 入口。

## 自动门禁

- `npm run check`
- 全 JavaScript / CJS 语法
- HTML 本地资源引用
- CSP / local-only 网络边界
- 生产 Admin / Cloud 私有能力排除
- RC13 local-only PDF permit 保留
- RC15 window capture 导出入口存在
- RC15 Formal Output Gate 直接进入链存在
- RC15 canonical PDF disabled 清理存在
- RC15 More hub 隐藏类自恢复与 drawer lifecycle observer 存在
- SOURCE / WINDOWS public runtime tree 逐文件一致
- ZIP CRC / 重解压树哈希一致
- Windows loopback server 关键页面 HTTP 200
- Protected PDF Core SHA256 不变

## Windows 实机终验

1. 打开 `•••`：必须立即看到“资料管理 / 单据辅助 / 高级”，不得出现空白抽屉。
2. 关闭再打开 `•••`，切换单据类型后再打开：正文仍在。
3. 顶部“导出 → 正式 PDF”：必须立即出现正式输出检查；不得无反馈。
4. 无阻断时确认继续可实际生成 PDF；有阻断时明确列出问题并可定位。
5. 2+ 页 PDF 页数与右侧预览一致。
6. 工作簿 Fit width / Whole page / +/- / More 工作表 / 左字段定位保持正常。
