(()=>{'use strict';
if(window.HUIDIAcquisitionBatchFusion)return;
const $=s=>document.querySelector(s),MAX_COMBINATIONS=12,MAX_KEYWORDS=4,MAX_MARKETS=4;
const PRODUCT_CACHE_KEY='huidi_online_product_brains_v1';
const MARKET_PRESETS=['United States','Germany','United Kingdom','France','Japan','United Arab Emirates','Australia','Canada','Netherlands','Poland'];
let busy=false,productRows=[];
function clean(v){return String(v??'').trim()}
function uniq(rows){const out=[];for(const value of rows.map(clean)){if(value&&!out.some(x=>x.toLowerCase()===value.toLowerCase()))out.push(value)}return out}
function esc(v){return clean(v).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});let payload=null;try{payload=await r.json()}catch(_){payload={detail:await r.text().catch(()=>r.statusText)}}if(!r.ok){const e=new Error(payload?.detail||r.statusText);e.status=r.status;e.code=payload?.code||'';e.payload=payload;throw e}return payload}
function readCachedProducts(){try{const rows=JSON.parse(localStorage.getItem(PRODUCT_CACHE_KEY)||'[]');return Array.isArray(rows)?rows:[]}catch(_){return[]}}
function productKeyword(row){return clean(row?.name||row?.product_name||row?.title||row?.keyword||row?.sku)}
function productMeta(row){return uniq([row?.sku,row?.category,row?.industry]).filter(Boolean).slice(0,2).join(' · ')}
async function loadProducts(){let rows=[];try{const server=await api('/api/product-brains');rows=Array.isArray(server)?server:[]}catch(_){rows=readCachedProducts()}if(!rows.length)rows=readCachedProducts();const seen=new Set();productRows=[];for(const row of rows){const keyword=productKeyword(row),key=keyword.toLowerCase();if(!keyword||seen.has(key))continue;seen.add(key);productRows.push(row);if(productRows.length>=24)break}renderProducts()}
function css(){if($('#huidiAcquisitionBatchCss'))return;const s=document.createElement('style');s.id='huidiAcquisitionBatchCss';s.textContent=`
.huidi-acq-batch{margin-top:8px;border:1px solid #e0e7ef;border-radius:11px;background:#fbfcfe}.huidi-acq-batch>summary{list-style:none;display:flex;align-items:center;gap:8px;padding:8px 10px;font-size:10px;font-weight:850;color:#38536e;cursor:pointer}.huidi-acq-batch>summary::-webkit-details-marker{display:none}.huidi-acq-batch>summary:after{content:'⌄';margin-left:auto;font-size:12px}.huidi-acq-batch[open]>summary:after{transform:rotate(180deg)}.huidi-acq-body{padding:0 10px 10px}.huidi-acq-help{font-size:9px;color:#7b8999;margin:0 0 8px}.huidi-acq-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:9px}.huidi-acq-group{border:1px solid #e5eaf0;border-radius:9px;background:#fff;padding:8px}.huidi-acq-head{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:6px}.huidi-acq-head b{font-size:9px;color:#39516a}.huidi-acq-head span{font-size:8px;color:#8b98a7}.huidi-acq-options{display:flex;gap:5px;flex-wrap:wrap;max-height:116px;overflow:auto;scrollbar-width:thin}.huidi-acq-option{display:inline-flex;align-items:center;gap:4px;border:1px solid #dfe6ee;border-radius:999px;padding:4px 7px;font-size:8px;color:#40566e;background:#fff;cursor:pointer}.huidi-acq-option:has(input:checked){border-color:#9ebff0;background:#edf4ff;color:#145aae}.huidi-acq-option input{width:12px;height:12px;margin:0}.huidi-acq-option small{color:#8d99a8;max-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.huidi-acq-actions{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-top:9px}.huidi-acq-actions button{height:31px;border:1px solid #1d63e9;border-radius:8px;background:#1d63e9;color:#fff;padding:0 10px;font-size:9px;font-weight:850;cursor:pointer}.huidi-acq-actions button:disabled{opacity:.55;cursor:default}.huidi-acq-actions .secondary{background:#fff;color:#45617d;border-color:#d7e0ea}.huidi-acq-count{font-size:9px;color:#60748a;font-weight:800}.huidi-acq-progress{font-size:8px;color:#7f8d9d;margin-left:auto}.huidi-acq-error{color:#b24343}.huidi-acq-empty{font-size:8px;color:#9aa5b1;padding:2px 0}.huidi-acq-status{font-size:8px;color:#718197;margin-top:7px;padding-top:7px;border-top:1px dashed #e3e8ef}.huidi-acq-status strong{color:#38536e}
@media(max-width:820px){.huidi-acq-grid{grid-template-columns:1fr}.huidi-acq-progress{width:100%;margin-left:0}}
`;document.head.appendChild(s)}
function renderProducts(){const box=$('#huidiBatchProducts');if(!box)return;if(!productRows.length){box.innerHTML='<span class="huidi-acq-empty">还没有可复用的产品资料；可直接使用上方当前关键词。</span>';return}box.innerHTML=productRows.map((row,i)=>{const keyword=productKeyword(row),meta=productMeta(row);return `<label class="huidi-acq-option"><input type="checkbox" data-huidi-batch-product="${i}" value="${esc(keyword)}"><span>${esc(keyword)}</span>${meta?`<small>${esc(meta)}</small>`:''}</label>`}).join('')}
function renderMarkets(){const box=$('#huidiBatchMarkets');if(!box)return;box.innerHTML=MARKET_PRESETS.map((market,i)=>`<label class="huidi-acq-option"><input type="checkbox" data-huidi-batch-market="${i}" value="${esc(market)}"><span>${esc(market)}</span></label>`).join('')}
function selectedKeywords(){const picked=[...document.querySelectorAll('[data-huidi-batch-product]:checked')].map(x=>x.value),current=clean($('#keyword')?.value);return uniq([...picked,current]).slice(0,MAX_KEYWORDS)}
function selectedMarkets(){const picked=[...document.querySelectorAll('[data-huidi-batch-market]:checked')].map(x=>x.value),current=clean($('#country')?.value);return uniq([...picked,current]).slice(0,MAX_MARKETS)}
function combinations(){const keywords=selectedKeywords(),markets=selectedMarkets();if(!keywords.length)return[];const targetMarkets=markets.length?markets:[''];const rows=[];for(const keyword of keywords)for(const country of targetMarkets)rows.push({product_keyword:keyword,country});return rows}
function updateCount(){const combos=combinations(),count=$('#huidiBatchCount'),run=$('#huidiBatchRun');if(!count||!run)return;const tooMany=combos.length>MAX_COMBINATIONS;count.textContent=combos.length?`已选 ${combos.length} 组搜索组合${tooMany?`，最多 ${MAX_COMBINATIONS} 组`:''}`:'选择产品和市场后批量搜索';count.classList.toggle('huidi-acq-error',tooMany);run.disabled=busy||!combos.length||tooMany}
function setProgress(text,error=false){const el=$('#huidiBatchProgress');if(!el)return;el.textContent=text||'';el.classList.toggle('huidi-acq-error',!!error)}
function providerStatusText(status){if(!status)return'真实数据来源状态未知';const company=[];if(status.serper)company.push('Serper');if(status.tavily)company.push('Tavily');const contact=[];if(status.hunter)contact.push('Hunter');if(status.serper)contact.push('Serper');return `客户搜索：${company.length?company.join(' → '):'未连接'} · 联系人：${contact.length?contact.join(' → '):'未连接'}`}
async function refreshProviderStatus(){const el=$('#huidiBatchProviderStatus');if(!el)return;try{const status=await api('/api/acquisition/status');el.innerHTML=`<strong>真实数据源</strong> · ${esc(providerStatusText(status))}${status.live_company_search?'':' · 未连接时不会生成模拟客户'}`}catch(_){el.textContent='真实数据源状态暂时无法读取；系统不会用本模块生成模拟客户。'}}
async function runBatch(){if(busy)return;const combos=combinations();if(!combos.length||combos.length>MAX_COMBINATIONS){updateCount();return}const buyerType=clean($('#buyerType')?.value)||'importer distributor wholesaler';busy=true;updateCount();let success=0,failed=0,totalItems=0;try{for(let i=0;i<combos.length;i++){const combo=combos[i];setProgress(`正在搜索 ${i+1}/${combos.length} · ${combo.product_keyword}${combo.country?' · '+combo.country:''}`);try{const out=await api('/api/leads/search',{method:'POST',body:JSON.stringify({...combo,buyer_type:buyerType,limit:6})});success+=1;totalItems+=Array.isArray(out?.items)?out.items.length:0}catch(e){failed+=1;if(e.code==='live_acquisition_provider_required'){setProgress(e.message,true);break}setProgress(`第 ${i+1} 组未成功：${e.message}`,true)}}await window.HUIDILeadWorkbench?.refresh?.();window.HUIDIWorkflowUsabilityClosure?.refresh?.();if(success)setProgress(`完成 ${success} 组${failed?`，${failed} 组未成功`:''} · 新返回 ${totalItems} 条真实客户结果`);else if(!$('#huidiBatchProgress')?.textContent)setProgress('本次没有完成有效搜索。',true)}finally{busy=false;updateCount()}}
function clearSelection(){document.querySelectorAll('[data-huidi-batch-product],[data-huidi-batch-market]').forEach(x=>x.checked=false);updateCount();setProgress('')}
function mount(){const searchGrid=$('.search-grid');if(!searchGrid||$('#huidiAcquisitionBatch'))return;css();const details=document.createElement('details');details.id='huidiAcquisitionBatch';details.className='huidi-acq-batch';details.innerHTML=`<summary>批量找客户 <span style="font-weight:600;color:#8996a5">复用产品资料 · 多产品 × 多市场</span></summary><div class="huidi-acq-body"><p class="huidi-acq-help">不用反复改关键词和国家。直接勾选已有产品与目标市场，系统按组合顺序调用同一个真实客户搜索链；最多 ${MAX_COMBINATIONS} 组，避免一次消耗过多搜索额度。</p><div class="huidi-acq-grid"><div class="huidi-acq-group"><div class="huidi-acq-head"><b>产品 / 关键词</b><span>最多 ${MAX_KEYWORDS} 个 · 自动包含上方当前关键词</span></div><div id="huidiBatchProducts" class="huidi-acq-options"><span class="huidi-acq-empty">正在读取已有产品…</span></div></div><div class="huidi-acq-group"><div class="huidi-acq-head"><b>目标市场</b><span>最多 ${MAX_MARKETS} 个 · 自动包含上方当前国家</span></div><div id="huidiBatchMarkets" class="huidi-acq-options"></div></div></div><div class="huidi-acq-actions"><button type="button" id="huidiBatchRun" disabled>批量搜索真实客户</button><button type="button" class="secondary" id="huidiBatchClear">清空勾选</button><span id="huidiBatchCount" class="huidi-acq-count">选择产品和市场后批量搜索</span><span id="huidiBatchProgress" class="huidi-acq-progress"></span></div><div id="huidiBatchProviderStatus" class="huidi-acq-status">正在读取真实数据源状态…</div></div>`;searchGrid.insertAdjacentElement('afterend',details);renderMarkets();loadProducts();refreshProviderStatus();details.addEventListener('change',updateCount);$('#keyword')?.addEventListener('input',updateCount);$('#country')?.addEventListener('input',updateCount);$('#huidiBatchRun').addEventListener('click',runBatch);$('#huidiBatchClear').addEventListener('click',clearSelection);window.addEventListener('huidi-product-brain-synced',()=>loadProducts());updateCount()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
window.HUIDIAcquisitionBatchFusion=Object.freeze({mount,refreshProducts:loadProducts,combinations,run:runBatch});
})();