# HUIDI Docs Community Local V1.2.0 RC16.23

## 本轮定位

Workspace R2：让第一次使用和每天高频使用都更直接。没有重写客户、商品、业务、单据数据键，也没有修改 Protected PDF Flow Core / Formal Output Gate。

## 导航与空间

- 左侧常用区只保留：首页、询盘 / 订单、客户、商品、单据。
- 产品目录、品牌 / 收款、条款模板、邮件、备份、回收站、帮助收进“更多工具”；进入这些页面时自动展开。
- 左侧宽度从 RC16.22 的 224px 进一步压到 204px；1280px 以下自动 190px。
- 业务 8 步流程只在首页显示，不再占用询盘 / 订单及其他管理页首屏。
- 顶部只保留“新询盘 / 做报价或单据 / 新建菜单”三类入口。

## 开箱即用

- 第一次打开会在首页显示 3 个准备项：公司 / 收款、第一个客户、第一个商品。
- 每项直接进入对应编辑窗口；完成后自动打勾，不使用强制向导或阻断式弹窗。
- 三项完成但还没有业务时，直接提供“记录第一条询盘 / 开始做单据”。

## 找资料

- 左侧新增跨模块查找：客户、商品、询盘 / 订单、单据，同时覆盖品牌、条款和邮件草稿。
- 支持客户名、联系人、SKU、HS Code、单号等关键词。
- 点击结果直接进入对应管理页；核心表格记录会自动定位并打开 RC16.22 详情抽屉。
- `Ctrl/Cmd + K` 可直接聚焦查找框。

## 日常安全

- 记住最近常用页面，已有业务数据的用户刷新后可直接回到上次工作位置；备份、回收站和帮助页不会被记为默认工作页。
- 左侧增加轻量备份状态，不弹窗、不阻断；有本地资料且 7 天未记录完整备份时才提示“建议备份”。
- 点击完整备份后记录本机备份时间，仅用于本机提醒。

## 保留边界

- RC16.22 Workspace R1 JS/CSS SHA 保持不变。
- Protected PDF Flow Core 与 Formal Output Gate SHA 保持不变。
- 客户 / 商品 / 询盘 / 单据存储键保持不变。
