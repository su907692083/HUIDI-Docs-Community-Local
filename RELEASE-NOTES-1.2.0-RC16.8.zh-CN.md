# HUIDI Docs Community Local V1.2.0 RC16.8

主题：Unified Document Layout Policy。

## 本轮修复

- 修复 RC16.7 中“标准 / 优先一页”仅报价单显示的问题。
- 报价单、形式发票、销售合同、商业发票、装箱单五类单据全部显示同一组“标准 / 优先一页”控件。
- 将分页模式从 quotation-only 状态提升为全单据统一 Layout Policy。
- Preview、正式 PDF DOM、Workbook/Table Preview 共用同一布局状态。
- Workbook/XLSX 打印设置同步读取“优先一页”；仅对不超过 48 行的适中工作表启用纵向一页拟合，长表仍保持安全分页，避免极端缩小。
- 保留 RC16.6.4/RC16.7 的现有分页完整性与本地数据链路，不修改单据业务数据 owner。
- 保留报价单旧 density key 作为兼容镜像，但不再由报价单独占布局策略。

## 行为定义

“优先一页”表示：在可读性允许时适度压缩间距、表格行高与签署区，尝试减少页数；内容较多时仍正常分页，不承诺强制一页。

## 需要真实 Windows 复核

请在 Edge / Chrome 下逐一检查 QT / PI / SC / CI / PL：
1. 五类单据均显示“标准 / 优先一页”。
2. 切换模式后 PDF 预览立即同步。
3. 切换到表格预览后布局模式保持一致。
4. 正式 PDF 导出结果与当前预览模式一致。
5. 长内容不会被强制压缩成不可读的一页。
