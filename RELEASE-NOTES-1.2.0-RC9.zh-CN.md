# HUIDI Docs Community Local V1.2.0 RC9

RC9 是针对 RC8 实机多页 PDF 回归的 WYSIWYG 导出收口版本。

## 本轮修复

- 将右侧实时 PDF 预览确立为唯一正式分页结果；导出过程不再重新 `renderPreview()`、不再重新分页。
- 导出时不再给当前预览附加 `pdf-exporting` 排版状态，不再改变 `#piPaper` 的高度、overflow 或页面布局。
- PDF 捕获对象从 `.pdf-page-body` 升级为完整 `.pdf-page`，逐页原样保留预览中的页边距、页脚、页码和版式。
- Community Local 的浏览器导出不再走可能将多页 DOM 压成单页的 `exportElementPdf` 旁路；统一走逐页捕获路径。
- 删除导出阶段额外绘制的第二套页眉、页脚和页码，避免“预览一套、下载又一套”。
- 新增三重页数硬门禁：`右侧预览页数 = 捕获 Canvas 页数 = jsPDF 页数`，任一不一致即停止保存，禁止静默生成缺页 PDF。
- 导出确认期间若预览 DOM 被其他模块重新渲染或页数发生变化，直接停止导出并提示重新确认预览。
- 继续保留 RC8 的内部工具、抽屉连通、收款资料直达、底部重复按钮移除和统一正式输出检查链。

## RC9 实机验收重点

1. 右侧预览显示 2 页时，下载 PDF 必须也是 2 页。
2. 第 1 / 2 页的模块归属、商品行、金额汇总、报价依据与右侧预览一致。
3. 不再出现第二页丢失、页尾截断、商品行重复、模块重复或 TOTAL 与下一分栏重叠。
4. PDF 页码必须与预览页码一致。
5. 导出前后的右侧预览不得跳页、重排或改变当前滚动内容。
