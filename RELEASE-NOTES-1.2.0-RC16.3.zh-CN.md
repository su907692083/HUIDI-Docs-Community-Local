# HUIDI Docs Community Local V1.2.0 RC16.3

RC16.3 是发布阻断修复与多语言统一候选。

## 本轮重点

- Community Local PDF 导出改为独立本地执行器，不再依赖历史按钮事件链或二次模拟点击。
- Edge / Chrome 支持 File System Access API 时，点击正式 PDF 立即进入系统“另存为”位置选择，然后离线生成并写入文件；不支持时回退到浏览器下载。
- PDF 导出过程提供明确的“准备 / 捕获页 / 保存 / 失败原因”状态，不再出现无反馈。
- PDF 仍以右侧 `.pdf-page` 为唯一分页来源，逐页冻结并用本地 html2canvas + jsPDF 生成。
- 17 种单语 + 中英双语继续保留，并新增共享单据词典桥。
- 表格编辑模式语言下拉恢复全部语言；客户工作簿预览、XLSX 表头、分栏和字段标签跟随当前输出语言。
- 修复表格模式原先只理解中文 / 英文 / 中英双语的问题。
- 补齐晚期新增字段/分栏的语义级翻译，避免“合同商品→商品明细”“箱数→总包装”“收货方→买方公司”等泛化错译。
- 表格模式的可见字段和分栏标签也跟随客户文件语言；B/L 等国际技术缩写不会被语言切分器拆开。
- 不自动翻译用户填写的业务正文；固定标题与字段走离线本地词典。

## 未改变

- 两份受保护 PDF 核心文件不修改。
- RC11 工作簿分页/缩放/定位核心不重写。
- 不加入云端翻译或 AI API。
