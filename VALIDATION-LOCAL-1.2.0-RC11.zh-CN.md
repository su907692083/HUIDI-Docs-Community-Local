# HUIDI Docs Community Local V1.2.0 RC11 验证记录

- [x] RC10 → RC11 最小差异施工
- [x] 默认表格预览改为适合宽度
- [x] 整页概览与手动缩放模式分离
- [x] 左侧字段 → 右侧对应单元格高亮/定位链保留并增强
- [x] 工作表标签“常用 + 更多”收口
- [x] PDF WYSIWYG 导出文件未修改
- [x] 本地运行资源引用检查
- [x] JavaScript 语法检查
- [x] Windows / Source public 运行树一致性检查（封包后）

## 实机验收重点

1. 浏览器保持 100% 缩放进入表格工作簿，默认应显示“适合宽度”，不再默认缩到约 46%。
2. 左侧点击字段时，右侧对应单元格应高亮并定位，文字保持可读。
3. 点击“整页”后应显示完整工作簿概览；点击“适合宽度”应恢复可读模式。
4. `+ / -` 手动缩放后不应被自动适配立即覆盖。
5. 工作表标签过多时，多余标签进入“更多”菜单且可正常切换。
6. 再导出两页 PDF，确认 RC9 多页导出不回退。

## 沙盒可验证 / 实机待验证

- 沙盒已完成：静态引用、JS 语法、Source/Windows `public` 一致性、RC11 缩放模式与按钮连接代码门禁。
- 当前沙盒 Chromium 被组织策略禁止访问 `127.0.0.1` / `file://`，无法替代真实 Windows 完成最终视觉截图。
- 正式可验证项：Windows 100% 浏览器缩放下的可读比例、左侧字段实时跟随高亮、工作表“更多”菜单、全屏核对，以及 PDF 回归测试。
