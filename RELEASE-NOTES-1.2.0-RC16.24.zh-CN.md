# HUIDI Docs Community Local V1.2.0 RC16.24

## 本轮定位

RC16.24 将原有“飞书协作快照”扩展为独立的 **Feishu Data Workspace / 飞书数据工作区**。目标不是后台扫描用户飞书，而是让用户在需要时主动读取自己自建应用有权访问的电子表格 / 多维表格，并复用到 HUIDI 客户与商品主数据，后续报价、PI、合同、CI、装箱单继续走既有本地主数据与单据快照链。

## 新增

- 左侧“更多工具”新增“飞书资料”。
- 客户中心、商品资料库新增“从飞书导入”。
- 支持粘贴飞书电子表格 Sheets 链接读取。
- 支持粘贴飞书多维表格 Bitable 链接读取。
- 支持配置文件夹 Token 后浏览当前应用实际有权访问的文件夹。
- 读取后先预览，不直接写入本地数据。
- 自动识别常见中文 / 英文字段名。
- 字段映射可人工修正并按“数据源 + 目标类型”保存在本机复用。
- 商品导入默认以 SKU，其次商品名 + 规格识别已有资料；客户默认以邮箱，其次公司名识别已有资料。
- 用户可选择匹配时更新已有主数据；每条导入资料保留 `feishu_source` 来源追踪。
- 最近使用的数据源保存在本机，不保存 App Secret。

## 权限与安全边界

- Community SOURCE / WINDOWS 包仍不包含真实 `config/feishu.local.json`。
- App Secret 仍只由本机 Companion Server 读取，不进入浏览器本地业务备份。
- HUIDI 只能读取用户自建应用实际有权限访问的文件，不绕过飞书本身的权限控制。
- 本轮不做后台全盘扫描；用户主动粘贴链接或浏览指定文件夹时才联网。
- 飞书源表变化不会静默修改历史单据；正式单据继续保持自己的业务快照。
- Wiki 知识库链接暂不自动解引用；需要打开实际 Sheets / Bitable 源表后复制链接。

## 保留能力

RC16.10–RC16.23 retained gates 继续保留。Protected PDF Flow Core 与 Formal Output Gate 不修改。
