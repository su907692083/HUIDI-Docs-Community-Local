# HUIDI Docs Community Local V1.2.0 RC11

RC11 聚焦表格工作簿预览的可读性与左右联动，不改动 RC9/RC10 已稳定的 WYSIWYG 多页 PDF 导出链。

## 本轮收口

- 表格工作簿默认由“整页塞入”改为“适合宽度”，优先保证文字可读。
- 新增独立“整页”概览按钮，需要查看全局结构时再缩小整张表。
- 左侧字段获得焦点时，右侧对应单元格持续高亮并自动定位；缩放过小时临时提升到可读比例。
- 手动 `+ / -` 后进入手动缩放，不再被自动适配立即覆盖。
- 工作表标签收为常用 4 个 + 当前页，其他页进入“更多”菜单，减少底部横向拥挤。
- 表格预览工具栏隐藏常驻纸张方向切换，纸张/布局继续由已有“布局”入口管理。
- 保留全屏核对。

## 未改动

- PDF 分页与 RC9 WYSIWYG 多页导出架构。
- Community Local 授权与本地数据模型。
