# HUIDI Docs Community Local V1.2.0 RC16.25

## 本轮定位

RC16.25 是 **Workspace Usability + Apple-like UI Closure**。不扩展新的业务模块，不修改 Protected PDF Core；重点根据 Windows 实机截图收口工作台首页、导航、客户/商品管理、空状态、弹窗和飞书/备份语义。

## 工作台与视觉

- 新增独立 `workspace-r3` 视觉作用域，加载顺序位于 RC16.22 / RC16.23 / RC16.24 之后，避免旧 Workspace CSS 与本轮 UI 互相争夺 owner。
- 浅色模式采用系统字体、浅灰工作区、半透明侧栏、弱边框、轻阴影和统一圆角；夜间模式保持同一组件层级。
- 左侧不再展示 `Community Local / RC` 版本卡；版本信息留给帮助/发布文件。
- 左侧四个并列快捷按钮收为单一 `＋ 新建` 菜单；高频导航继续保留首页 / 询盘订单 / 客户 / 商品 / 单据。
- 非首页隐藏重复的全局顶部动作，页面只保留当前业务的主动作；导入、导出、批量/次要动作收进 `•••`。
- 首页隐藏旧重复快捷卡，保留首次使用准备、待处理统计、业务组成和最近工作；8 步大卡片改为轻量业务链提示。
- 表格、筛选、按钮和信息卡统一密度，正文/表格字号提高到更适合长期办公阅读的范围。

## 客户建档

- 默认只显示公司、联系人、邮箱、电话/WhatsApp、国家、币种、来源、下次跟进。
- “懒人录入”改为可展开的 `粘贴客户资料自动识别`，不再默认占据大块首屏。
- 网站/语言/标签/地址/备注收进“更多客户资料”。
- 注册号、Tax ID、VAT、EORI 收进“税务与注册资料”。
- Bill To / Ship To / Consignee / Notify Party / 目的港收进“收货与通知资料”。
- 编辑已有客户时，如果高级分组已有值，对应分组自动展开，避免隐藏现有资料。

## 商品建档

- 默认优先显示：商品名称、SKU/型号、规格、参考价格、币种、单位、MOQ、HS Code。
- 商品图片区域压缩为紧凑布局，不再占据过大首屏。
- 海关申报品名、原产国、供应商收进“报关与供应资料”。
- 包装类型、外箱尺寸、装箱数、N.W.、G.W.、CBM、唛头收进“包装与物流”。
- 来源链接、视频、内部备注收进“来源与内部资料”。
- `箱规` 明确为 `外箱尺寸（长 × 宽 × 高 cm）`；当用户按 cm 输入三边且 CBM 为空时，可自动计算每箱 CBM。

## 飞书 / 备份语义

- “备份”页面只负责 JSON 恢复/迁移和 CSV 导出。
- 旧“飞书云文档协作同步”移动到“飞书资料”下的 `协作快照（可选）`，与 RC16.24 飞书表格数据源能力放在同一入口。
- 飞书数据源仍采用显式读取/映射/复用，不后台扫描，不静默改写历史单据快照。

## 保护边界

以下文件保持原 SHA256：

- `public/flypigbox-v3-3-2-3-pdf-flow-fix.js`
  `abb741448747b8161c9dfafff77a76f8cecd41771d436234424f3a43af275b36`
- `public/flypigbox-v3-3-6-24-r1-3a-18-formal-output-gate.js`
  `570884ad3445361c60b0ef491544f54adce689b89f92ffd546803b619cb93583`
