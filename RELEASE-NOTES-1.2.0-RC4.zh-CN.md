# HUIDI Docs Community Local V1.2.0 RC4

本轮是稳定性和界面结构收口，不扩展新的业务模块。

## P0 修复
- 移除 RC3 编辑器全 body MutationObserver 反馈循环，避免 Chrome/Edge 页面无响应。
- 本地编辑器 URL 统一 `type` 与历史 `doc` 参数，报价转 PI/合同/CI/装箱单不再出现类型冲突。
- 下一张单据恢复时强制把 `documentType` 写入 state 与 fields，避免上一张单据类型把目标类型覆盖回去。

## UI / UX
- Workspace 每个页面只保留一个正式页头；内部重复页头改为操作工具行。
- 8 步业务链改为 8 列，较窄屏幕按 4+4 / 2+2+... 响应式排列。
- 状态说明条压缩，不再每页占据大块纵向空间。
- 客户和商品空状态增加直接新增 / CSV 导入入口。
- 使用说明二维码区域改为可收缩栅格，避免右侧赞赏码被裁切。
- 继续收紧横向溢出；表格横向滚动限制在表格容器内部。

## 数据链
- 本地单据镜像补充 customer_id / deal_id 摘要，单据中心更容易保持客户关联。
