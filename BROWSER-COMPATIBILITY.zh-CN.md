# 浏览器与性能说明 — V1.2.0 RC6

## 推荐

- Microsoft Edge 最新稳定版
- Google Chrome 最新稳定版

## 其他浏览器

多数 Chromium 内核浏览器理论上可以运行，但以下行为可能存在差异：文件下载、打印、剪贴板权限、文件选择器、本地存储容量、PDF 排版。

如果遇到“PDF / XLSX / CSV 点击没反应、下载被拦截、打印版式不同”，第一步请在最新版 Edge 或 Chrome 复测。

## 为什么没有做重 3D

本版本的高级感主要通过 SVG 图标、渐变、阴影、卡片层次、轻微悬浮与过渡完成，不使用 WebGL、粒子系统、实时 3D 场景或视频背景。这样可以降低显卡和内存占用，也减少不同浏览器之间的兼容风险。

系统会尊重 `prefers-reduced-motion`，用户系统设置“减少动态效果”时会关闭大部分动画。
