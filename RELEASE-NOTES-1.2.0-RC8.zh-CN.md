# HUIDI Docs Community Local V1.2.0 RC8

RC8 是 RC7 实机验收后的交互与 PDF 导出收口版本。

## 本轮修复

- 移除报价单底部重复的“保存草稿 / 检查资料 / 导出PDF”固定栏，避免遮挡表单、抽屉和底部字段；统一使用顶部固定工具栏。
- 修复 Community Local 的“内部工具”按钮点击无反应：本地工厂执行 / 成本核算工作台恢复可用，保持纯本地运行。
- “更多工具”补充常用工具入口，并与字段设置、布局、收款资料、签名公章、内部工具共用同一状态链。
- 修复“展开 / 收起”文字在窄抽屉中纵向断行。
- 正式导出只保留一套 Formal Output Gate；抑制旧翻译核对弹窗与跨模块检查重复串联。
- 顶部“检查”直接打开统一正式输出检查，不再跳到隐藏旧入口。
- 修复 RC7 PDF 捕获回归：不再使用 scrollWidth / scrollHeight 捕获页外溢出内容，避免补充信息重复、商品行重复、总计与下一分栏重叠。
- PDF 导出改为严格捕获已通过分页门禁的真实 `.pdf-page-body` 页面盒，与右侧预览保持同一分页边界。

## RC8 实机验收重点

1. 报价单滚动到底部时不再出现遮挡操作的固定三按钮。
2. 点击顶部“内部工具”可以打开本地内部工作台并正常关闭。
3. “更多”中的字段、布局、收款、签名、内部工具均能到达对应功能，不产生叠层死路。
4. 点击“检查”或“导出”只出现一套正式输出检查。
5. 导出两页及以上 PDF：补充信息、商品行不重复；总计、报价依据等区块不重叠；页尾不截断。
